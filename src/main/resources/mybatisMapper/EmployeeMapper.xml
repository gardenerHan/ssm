<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ifox.hgx.mybatis.dao.EmployeeMapper">

    <select id="getEmpById" resultType="com.ifox.hgx.mybatis.entities.Employee">
        SELECT * FROM tab_employee WHERE id=#{id}
    </select>

    <select id="getEmpAll" resultType="com.ifox.hgx.mybatis.entities.Employee">
        SELECT * FROM tab_employee
    </select>

</mapper>